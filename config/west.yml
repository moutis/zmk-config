manifest:
  remotes:
    - name: zmkfirmware
      url-base: https://github.com/zmkfirmware
#    - name: caksoylar ## For zen display/power fixes
#      url-base: https://github.com/caksoylar
    - name: urob
      url-base: https://github.com/urob
#    - name: ssbb
#      url-base: https://github.com/ssbb
  projects:
    - name: zmk
      remote: zmkfirmware
#      revision: main

#      remote: caksoylar ## For zen display/power fixes
#      revision: caksoylar/zen-v1+v2

#      remote: urob # fixes for zmk-adaptive-key
      revision: v0.2+fix-child-nodes # v0.2+fix-child-nodes or main

      import: app/west.yml

    # Modules used
    - name: zmk-adaptive-key
      remote: urob
      revision: v0.2 # set to same as ZMK version above (main or v0.2)
#      revision: main # set to same as ZMK version above (main or v0.2)

#    - name: zmk-antecedent-morph
#      remote: ssbb
#      revision: v1
  self:
    path: config
